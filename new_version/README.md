# AI Pharmacy v2

Windows-native, lightweight, scalable pharmacy system.

## Setup
1. `cd new_version`
2. `npm install`
3. Create a `.env` file with:
   ```
   TELEGRAM_TOKEN=your_token_here
   ```
4. `npm start`
